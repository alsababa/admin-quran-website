/** @type {import('next').NextConfig} */
const nextConfig = {
    // Temporarily disabled for debugging 500 error
    // output: 'export',
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
